namespace TiendaMusica.Domain.Models.Result
{
    public enum ErrorCode
    {
        SERVER_ERROR,
        CONFLICT_ERROR,
        VALIDATION_ERROR,
        NOT_FOUND,
        PUBLISH_MESSAGE_ERROR,
        DATABASE_ERROR
    }
}
